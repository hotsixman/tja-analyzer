import { HitNote } from "tja-parser";

export function getPatternComplexity(group: HitNote[]){
    
}

export function subgroupByRepetition(group: HitNote[]){
    for(let l = 2; l < Math.ceil(group.length / 2); l++){
        
    }
}